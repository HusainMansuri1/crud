interface EmpDetails {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  contactNumber: string;
  dob: Date;
};

interface Fields {
  title: string;
  dataIndex: string;
  key: string;
  inputType: string;
  editable: boolean;
};